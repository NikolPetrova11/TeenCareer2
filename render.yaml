services:
  - type: web
    name: teencareer2
    runtime: node
    buildCommand: chmod +x render-build.sh && ./render-build.sh
    startCommand: npm run server
    envVars:
      - key: NODE_ENV
        value: production
      # No Puppeteer/Chromium required for PDF generation
